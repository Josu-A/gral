#ifndef CODE_HEADER_H
#define CODE_HEADER_H

#include <stdbool.h>

/**
 * Returns true if the integer x reads the same forwards and backwards.
 * Negative numbers and non-zero multiples of 10 are never palindromes.
 */
bool isPalindrome(int x);

#endif
